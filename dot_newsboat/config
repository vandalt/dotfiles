prepopulate-query-feeds yes
color info black blue bold
color listfocus black blue
color listfocus_unread black blue bold
browser firefox
macro i set pager /home/vandal/.local/bin/kitty-img-pager; open; set pager internal

unbind-key g # bound to `sort` by default
bind-key g home

unbind-key G # bound to `rev-sort` by default
bind-key G end

unbind-key h
unbind-key j
unbind-key k
unbind-key l

bind-key h quit
bind-key j down
bind-key k up
bind-key l open
